/target
**/*.rs.bk
.env
/data/*.db
/data/raw/
.gstack/

.vercel
.env*

# Generated intermediates — the .docx is the deliverable, the .html is scaffolding
docs/**/*.html
# ...except the deck, which is the opposite case: the compiled HTML *is* the
# deliverable and cannot be regenerated (fill_deck.py's assets are gone), so it
# is the artifact of record rather than scaffolding. See docs/deck/README.md.
!docs/deck/HouseCheck-Presentation.html

# Deliberately untracked: session scratch and the two review documents that are
# Aisling's alone to publish. These were swept in once by a `git add -A`; the
# ignore entries make that mistake unavailable rather than merely corrected.
.playwright-mcp/
.remember/
HouseCheck_Claim_Verification_Dossier.md
HouseCheck_Data_Integrity_Review.pptx
